<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>
<?import javafx.geometry.Insets?>
<?import javafx.geometry.Pos?>

<BorderPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="ma.enst.iibdcc.chatClient.ChatClientController"
            prefWidth="600" prefHeight="500"
            style="-fx-background-color: #ece5dd;">

    <!-- En-tête -->
    <top>
        <HBox spacing="10" alignment="CENTER_LEFT"
              style="-fx-background-color: #075E54; -fx-padding: 10;">
            <Label text="💬 Chat Room"
                   style="-fx-text-fill: white; -fx-font-size: 18px; -fx-font-weight: bold;" />
        </HBox>
    </top>

    <!-- Zone des messages -->
    <center>
        <ScrollPane fx:id="scrollPane" fitToWidth="true"
                    style="-fx-background: #ece5dd;" >
            <VBox fx:id="messageContainer"
                  spacing="10"
                  style="-fx-background-color: #ece5dd;">
                <padding>
                    <Insets top="10" right="10" bottom="10" left="10"/>
                </padding>
            </VBox>

        </ScrollPane>
    </center>

    <!-- Zone d’envoi -->
    <bottom>
        <HBox spacing="10" alignment="CENTER_LEFT" style="-fx-background-color: #ffffff;">
            <padding>
                <Insets top="10" right="10" bottom="10" left="10"/>
            </padding>
            <TextField fx:id="inputField"
                       promptText="Tapez un message"
                       HBox.hgrow="ALWAYS"
                       style="-fx-background-radius: 20; -fx-padding: 8; -fx-font-size: 14px;" />
            <Button text="➤" onAction="#handleSend"
                    style="-fx-background-color: #25D366; -fx-text-fill: white; -fx-background-radius: 20; -fx-font-size: 16px; -fx-font-weight: bold; -fx-padding: 8 16;"/>
        </HBox>
    </bottom>
</BorderPane>
